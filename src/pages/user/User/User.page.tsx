import { Card } from '@mantine/core';
import { DataTable } from 'mantine-datatable';

export const UserPage = () => {
  return (
    <Card m="xs" withBorder>
      <DataTable columns={[]} records={[]} />
    </Card>
  );
};
